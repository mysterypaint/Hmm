#pragma once
/*

#include <map>
#include <string>
#include "Vector2D.hpp"
#include "ECS\ECS.hpp"
#include "PHL.hpp"

class AssetManager {
public:
	AssetManager(Manager* _manager);
	~AssetManager();

	void AddTexture(int _group, int _id);
	PHL_Surface* GetTexture(int _id);
private:
	Manager* manager;
	std::map<int, PHL_Surface*> textures;
};

*/